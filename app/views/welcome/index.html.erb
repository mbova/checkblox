<div class="inner cover">
  <h1 class="cover-heading">Get stuff done.</h1>
  <p class="lead">Manage only your most important priorities in one list.</p>
  <p class="lead">
  	<% if current_user %>
    	<a href="/items" class="btn btn-lg btn-default">My List</a>
    <% else %>
    	<p class="lead"><%= link_to "Sign In", new_user_session_path %>
    	</p>
    	<p>or</p>
        <%= link_to "Sign Up", new_user_registration_path, :class => "btn btn-lg btn-default" %>
    <% end %>
  </p>

</div>