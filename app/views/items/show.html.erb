<p id="notice"><%= notice %></p>

<table class="table">
	<tr>
		<thead>
	  	<th>Name</th>
	  	<th>Description</th>
	 	</thead>
	</tr>
 	<tbody>
 		<tr>
			<td><%= @item.name %></td>
  		<td><%= @item.description %></td>
  	</tr>
  </tbody>
</table>

<%= link_to 'Edit', edit_item_path(@item) %> |
<%= link_to 'Back', items_path %>